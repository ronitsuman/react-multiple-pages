npm i react-router-dom

import {Route , Routes } from "react-router-dom"

<header>
<Routes>
<Route path = "/" element={<Home />}
<Route path = "/about" element={<About/>}
<Route path = "/contact" element={<Contact/>}
<Route path = "/blogs" element={<Blogs/>}
</Routes>
</header>


link in header tags 
main me browser router ke beech me app daalna hita hita


routing 3 type  process  -
 const App = ()=>{
    let router = createBrowserRouter([
        {
            path: "/",
            elements:(
                <>
                <Header/>
                <Home/>
                <>
            ),
        },{
            path : "/about"
            element: (
                <>
                <Header/>
                <About/>
                <>
            ),
        },
        },{
            path : "/contact"
            element: (
                <>
                <Header/>
                <contact/>
                <>
            ),
        },
        },{
            path : "/blogs"
            element: (
                <>
                <Header/>
                <Blogs/>
                <>
            ),
        },
    ]);
    return (
        <>
        <RouterProvider router={router}/> 
        </>
    )
 }


 link vs nav link 
 1. link me is active ka use ni kr sk skte hai
2..navlink me classname me is active function lagane hai 
3. 



old method ...... V
  

    const [value,setValue]=useState(0)

  const handleNavClick = (event,newValue)=>{
    event.preventDefault();  
    setValue(newValue)
  }


  <div className='w-[100vw] px-6 py-4 bg-blue-700'>
       <nav className='flex list-none gap-5 text-white justify-end'>
        <li><a onClick={(event)=>handleNavClick(event , 1)}  href="/">Home </a></li>
        <li><a onClick={(event)=>handleNavClick(event , 2)} href="/bout">About</a></li>
        <li><a onClick={(event)=>handleNavClick(event , 3)} href="/contact">Contact </a></li>
        <li><a onClick={(event)=>handleNavClick(event , 4)} href="/blog">Blogs </a></li>
        </nav>  
      </div>

      {value ===1 && <Home/>}
      {value === 2 && <About/>}
      {value === 3 && <Contact/>}
      {value === 4 && <Blog/>} 



new Method -